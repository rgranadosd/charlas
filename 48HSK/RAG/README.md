# RAG - Retrieval Augmented Generation

Directory containing RAG (Retrieval Augmented Generation) system implementations for natural language processing and semantic search.

## 📋 Content

This directory contains RAG-related projects:

### 📚 python-rag

Complete RAG system implemented in Python that combines:
- Local embedding models (Mistral E5 Multilingual)
- LLM APIs (Groq and OpenAI)
- Document processing
- Semantic retrieval system

**Location:** `python-rag/`

## 🚀 Quick Start

To use the RAG system, consult the specific documentation for each project:

- **python-rag:** See `python-rag/README.md`

## 📝 Notes

- Each project has its own virtual environment
- Credentials are stored in `.env` files (protected by `.gitignore`)
- Consult each project's documentation for installation and usage instructions
